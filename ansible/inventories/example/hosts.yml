---
all:
  children:
    postgresql_servers:
      hosts: {}
    timescaledb_servers:
      hosts: {}
    nginx_servers:
      hosts: {}
    zabbix_servers:
      hosts: {}
    zabbix_agents:
      hosts: {}

# Add real hosts in an environment-specific inventory before running.
# A host may belong to several groups; for example a Zabbix VM can also be
# a PostgreSQL, TimescaleDB and Nginx host.

---
# Safe example defaults only. Do not place real passwords or tokens here.
postgresql_version: 17
postgresql_listen_addresses: "localhost"
postgresql_port: 5432
postgresql_databases: []
postgresql_users: []
postgresql_hba_rules: []

timescaledb_databases: []
timescaledb_run_tune: false

nginx_remove_default_site: true
nginx_sites: []

zabbix_version: "7.0"
zabbix_agent_port: 10050

# Define these in an environment inventory / Ansible Vault when used:
# zabbix_server_address: "monitoring.example.internal"
# zabbix_server_db_password: !vault |
#   <encrypted value>
